kind: work
version: 1
id: replace-me
title: Replace me
status: draft
baseline: replace-with-git-revision
strict_verification: true
scope_control:
  standard: docs/plan-scope-control-standard.yaml
  reused_verified_plans: []
  baseline_only_surfaces: []
  residual_scope: []
  retest_triggers: []
  non_duplication_rule: Prior verified evidence is reused as baseline and is not counted as new completion.
# For non-trivial work, record the advisory System Map report consumed during closeout.
# This is review evidence only and never replaces verifier status.
change_impact_review:
  required: false
  report: docs/audit-output/system-map-change-impact.json
  disposition: pending
# Allowed dispositions: reviewed, deferred_with_backlog_id, not_applicable_with_reason.
# For high-risk masters, add serial_task_execution and execution_inventory so
# each task must be explicitly started and independently verified.
# serial_task_execution: true
# execution_inventory: docs/work/<master>-execution-inventory.yaml
tasks:
  - id: task-1
    title: Replace me
    type: implementation
    status: pending
    observable_outcome: Replace with one independently verifiable outcome.
    dependencies: []
    evidence_boundary: [targeted tests or explicit evidence type]
    paths: []
    required_paths: []
    # requires_visual_evidence: true
    # visual_evidence_paths: []
    # manual_runtime_command: replace-with-project-runtime-command
    # runtime_evidence_paths: []
    validation: "make audit-todo"
evidence: []
