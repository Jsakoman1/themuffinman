# Implementation Backlog

2026-07-18 search comparison closeout: backend comparison now returns privacy-safe family-specific owner, member, application, location, and availability fields plus explicit omitted-selection fallback for denied/empty Web selections; Vision ordinal/typed selection and the combined Web/Vision runtime trace now pass. The atomic comparison capabilities are verified; broader search ranking/pagination remains outside this closeout.

2026-07-18 Vision pause/resume reconciliation: existing enabled and default-disabled browser evidence plus backend transition tests now prove `work.quest.pause` and resume review/confirmation, participant notification, state change, and safe review-only behavior. The atomic pause capability is implemented; worker reassignment and broader Vision gap-02 work remain active.

2026-07-18 Vision reopen closeout: normalized `reopen quest` routing and confirmation now have 105 passing targeted backend tests plus fresh execution-enabled browser evidence with an approved applicant (`OPEN → ASSIGNED → IN_PROGRESS → COMPLETED → Vision review → confirmation → OPEN`), including application reset to `PENDING`. The atomic `work.quest.reopen` capability is verified; the parent Vision gap/master remain active.

2026-07-19 reconciliation metadata correction: regenerated `docs/work/target-capability-coverage-report.yaml` from the canonical inventory, reconciled `vision-gap-08` with the verified Rides production plan, and added the verified Web/Vision comparison atom. Coverage is now 123/123 mapped with 1 Web and 2 Vision gaps, and the Vision backlog reports 9 verified capability atoms plus 10 pending; the three top-level redesign TODOs remain active.

2026-07-18 Rides collection surface remediation progress: `RidesView` now applies semantic graphite geometry to matching filters and the ride workspace while preserving consent, circle visibility, and lifecycle validation; fresh route-archetype evidence passed for desktop and narrow layouts.

2026-07-18 plan metadata reconciliation: workspace redesign master, execution inventory, canonical register, backlog, and `docs/work/workspace-redesign-reconciliation-report.yaml` now agree on 103/103 verified serial items and three canonical residual TODO IDs. Nested parity boundaries and the 19-capability Vision backlog are explicitly mapped under those three IDs; resolved named scopes remain closed and are not reopened. Master and execution inventory remain active.
2026-07-18 control metadata audit: master and execution inventory timestamps now reflect the latest plan/docs/runtime/recursion audits; audit scope is explicitly metadata/control-only, so no aggregate master evidence or TODO was falsely closed.

2026-07-18 visual migration progress: Rides matching filters and offer/edit dialog now use shared `AppFormField`/`AppFormFooter` primitives; type-check, modern-surface audit, build, and route-archetype evidence passed. Master plan remains active.
2026-07-18 shell visual remediation progress: `AccountMenu` and remaining authenticated-shell fallback controls now use shared graphite control/surface tokens, semantic radii, focus treatment, and overlay shadows; backend behavior is unchanged and fresh route-archetype evidence passed for desktop and narrow layouts after a clean stack restart.
2026-07-18 shared interaction-surface remediation progress: `AppActionDialog`, `QuickSwitcher`, and `WorkspaceKeyboardHelp` now use the shared graphite overlay/control contract with semantic radii, focus states, and quiet hover treatment; keyboard and action behavior remain unchanged and fresh route-archetype evidence passed for desktop and narrow layouts.
2026-07-18 shared state/dialog remediation progress: `AppStatus` retry actions and `AppDialog` close controls now use shared compact controls, focus treatment, and semantic dialog geometry; async state and dialog contracts remain unchanged and fresh route-archetype evidence passed for desktop and narrow layouts.
2026-07-18 Business Availability visual remediation progress: the availability collection and editor now use direct semantic graphite tokens for dense rows, preview, form controls, and responsive layout; legacy pill/radius/light fallback declarations were removed while backend schedule and conflict validation remain unchanged, and fresh route-archetype evidence passed for desktop and narrow layouts.
2026-07-18 Business Offerings visual remediation progress: the offerings collection and editor now use direct semantic graphite tokens for dense rows, preview, form controls, and responsive layout; legacy pill/radius/light fallback declarations were removed while backend pricing, duration, and booking validation remain unchanged, and fresh route-archetype evidence passed for desktop and narrow layouts.
2026-07-18 Business Bookings visual remediation progress: the booking collection, server-gated action controls, selected preview, and reschedule form now use direct semantic graphite tokens; legacy pill/radius/light fallback declarations were removed while allowed-actions and server-side conflict validation remain unchanged, and fresh route-archetype evidence passed for desktop and narrow layouts after a clean retry.
2026-07-18 Rides visual remediation progress: the rides collection, matching filters, privacy-safe commute form, workspace rows, preview, and offer/edit form now use direct semantic graphite tokens; legacy pill/radius/light fallback declarations were removed while consent, circle visibility, and ride lifecycle rules remain unchanged, and fresh route-archetype evidence passed for desktop and narrow layouts.
2026-07-18 master verification reconciliation: `make work-verify plan=docs/work/workspace-redesign-complete-master.yaml` generated a candidate aggregate evidence entry and verified the 103-item serial inventory, but the master was intentionally kept `active` because `master_evidence_status` remains missing and the canonical product/reconciliation TODO queue remains open. No master closeout was claimed.
2026-07-18 Chat source visual remediation progress: `ChatSurfaceView` base styles were consolidated onto direct semantic graphite three-column tokens for index, thread, context rail, composer, direct/group create, message actions, and narrow reflow; legacy pill/radius/light gradient/shadow fallbacks were removed while realtime sync, attachments, and recovery contracts remain unchanged, and fresh route-archetype evidence passed for desktop and narrow layouts.
2026-07-18 Vision parity evidence progress: authenticated `run-vision-handoff-runtime-evidence.mjs` passed typed Work -> Vision handoff, backend turn response, safe Return link, original collection filter retention, and desktop screenshot capture; Vision parity and master reconciliation remain active.
2026-07-18 Vision smoke boundary: an earlier full `web-runtime-smoke` reached worker replacement review but stopped at the configured execution-disabled response while the harness expected an executed mutation; the harness was later branched to prove both enabled and default-disabled lifecycle modes.
2026-07-18 visual migration progress: Business and People discovery search/filter forms now use the shared `AppSearchField` primitive with standardized label, loading, submit, keyboard, and graphite control behavior; type-check, modern-surface audit, build, and route-archetype evidence passed after a clean backend-startup retry. Master plan remains active.
2026-07-18 visual migration progress: Work collection workspace/list/status surfaces now use semantic surface radii and graphite control/background tokens instead of legacy card/light literals; type-check, modern-surface audit, build, and route-archetype evidence passed after a clean backend-startup retry. Master plan remains active.
2026-07-18 visual migration progress: shared `SurfaceContentView` metric, status, calendar, badge, and hover surfaces now use graphite tokens, semantic radii, and quiet no-lift interaction treatment; type-check, modern-surface audit, build, and route-archetype evidence passed after a clean backend-startup retry. Master plan remains active.
2026-07-18 visual migration progress: authenticated workspace shell now explicitly owns graphite canvas/rail/frame/header tokens and a distinct accent mobile Vision state, preserving the stable navigation shell and responsive drawer behavior; type-check, modern-surface audit, build, and route-archetype evidence passed after a clean backend-startup retry. Master plan remains active.
2026-07-18 interaction/accessibility progress: authenticated shell rail resizer now supports keyboard Arrow/Home/End resizing with Shift step control, focus-ring feedback, ARIA range values, and persistence through the existing backend workspace preference endpoint; type-check, modern-surface audit, build, and route-archetype evidence passed after a clean backend-startup retry. Master plan remains active.
2026-07-18 collection interaction progress: Work discovery now uses the shared `DisplayDensityControl` and passes compact/default/comfortable density into `SurfaceRow`; legacy boolean density state remains backward-compatible in session storage, and the prior ineffective AppCard-only comfortable-row selector was removed. Type-check, modern-surface audit, and docs audit pass. Master plan remains active.
2026-07-18 shared-surface adoption progress: Activity now renders a reusable context rail beside its dense collection, section hubs use `CollectionContextRail`, and icon actions use the delayed accessible `AppTooltip`; remaining stale `AppCard` is intentionally retained as an isolated opt-in card primitive, not a default collection surface. Master plan remains active.
2026-07-18 Work detail cleanup progress: `WorkQuestDetailView` now has one consolidated token-based style contract instead of layered legacy radius/card overrides; detail utility, facts, forms, review, mobile reflow, and status states all use shared graphite geometry. Type-check, modern-surface audit, and diff validation pass. Master plan remains active.
2026-07-18 Work detail evidence progress: fresh `run-route-archetype-evidence.mjs` completed successfully after the Work detail cleanup; the intentionally started stack was explicitly terminated and ports 8080/5173 confirmed free. Master plan remains active.
2026-07-18 dev lifecycle hardening: root `make dev-doctor` now forwards to the app stack doctor, and `dev-stack.sh stop` waits for owned child processes then fails truthfully when 8080/5173 remain occupied instead of silently removing state. A clean `make dev-doctor` and `make dev-stop` pass with both ports free. Master plan remains active.
2026-07-18 runtime acceptance progress: authenticated `web-runtime-smoke` now passes density switching, selected-row ArrowDown URL state, P preview, full worker replacement/release, and cross-module flows. Owned process-tree shutdown was corrected to terminate Java/Vite descendants through trusted parentage; an escalated start/stop verification leaves both ports free. Master plan remains active.
WORKSPACE-CLOSEOUT-EVIDENCE-RECONCILIATION [ACTIVE]: Reconcile strict closeout plans with current runtime artifacts. Fresh workspace visual, Vision handoff, and route-archetype captures now exist under `docs/runtime-evidence/workspace-redesign`; matching final artifacts were materialized under `docs/runtime-evidence/linear-redesign`. The repository Makefile does forward `task` to `work-verify`; the remaining work is to perform a task-aware verification against a clean, isolated baseline and record the resulting changed-path/evidence fingerprints without bypassing active route/Vision gaps.
2026-07-18 worker lifecycle contract closeout: backend `allowedActions`, Web Release/Replace controls, Vision review/confirmation, enabled execution, default-disabled review-only safety, recovery responses, and a targeted narrow Web capture now agree for the named worker lifecycle contract. Broader Web/Vision parity remains tracked separately.
2026-07-18 Vision lifecycle progress: with the explicit dev execution flag enabled, authenticated `web-runtime-smoke` now also proves Vision worker replacement review → confirmation → backend execution, followed by Web release; the default disabled flag remains covered as a truthful review-only state. Vision gap closure remains active.
2026-07-18 persisted Vision lifecycle evidence: the corrected authenticated enabled smoke passed the full cross-module sequence and wrote `docs/runtime-evidence/workspace-redesign/web-runtime-smoke-enabled.json`; the smoke assertion now scopes Activity dismissal to the canonical list surface because the same route is intentionally rendered in the context rail.
2026-07-18 default-disabled Vision lifecycle evidence: the branched authenticated smoke passed with the production-safe default (`SIDEQUEST_VISION_EXECUTION_ENABLED` unset/false), proving worker replacement and quest pause/resume stop at review with explicit disabled responses and no mutation; persisted at `docs/runtime-evidence/workspace-redesign/web-runtime-smoke-default-disabled.json` with a targeted narrow lifecycle capture. Broader parity remains open.
2026-07-18 permission/access recovery evidence: independent authenticated traces now prove geolocation denial -> backend place lookup recovery -> approximate save/reload, Chat reconnect/resync, and backend-authorized exact-location visibility for an accepted contact versus an outsider. `WEB-AUTH-RECOVERY-TRACE` is resolved for its named evidence scope; broader parity remains active.
2026-07-18 Business booking handoff evidence: independent owner/customer browser trace created backend offering and availability, rendered canonical public detail, accepted booking preview, created a booking request, and captured narrow reflow in `docs/runtime-evidence/workspace-redesign/business-booking-handoff-trace.json`. `WEB-BUSINESS-BOOKING-HANDOFF` is resolved for its named evidence scope; broader Web functional completion remains active.
2026-07-18 Vision pause/resume progress: fixed the central `CONFIRM_REVIEW` dispatcher so `PAUSE_QUEST` and `RESUME_QUEST` reach their typed execution adapter; enabled authenticated smoke now proves separate review → confirmation → state transition flows for pause and resume. Vision gap closure remains active.
2026-07-18 shared-surface visual progress: `SurfaceContentView`, `SurfaceHeader`, `SurfaceMetricGrid`, `SurfaceSection`, and the global Vision entry now use shared graphite control/surface tokens for geometry, emphasis, calendar controls, voice entry, and status dots; legacy pill radii, lift shadows, light fallbacks, and hardcoded accent colors were removed from these shared layers. Type-check, build, modern-surface contract, static legacy audit, diff validation, and fresh desktop+narrow route-archetype evidence pass; route-level visual remediation is now closed for the named app-shell scope.
2026-07-18 Vision confirmation routing progress: central `CONFIRM_REVIEW` now dispatches chat-read, notification-read/all-read, and notification-preference intents to their existing typed execution handlers, eliminating another review-to-409 gap. Existing conversation, execution-service, and adapter tests pass; Vision parity remains active pending end-to-end evidence.
2026-07-18 Vision notification routing progress: explicit mutation-signal precedence now routes `mark all notifications as read` and `disable chat in-app notifications` away from semantic `VIEW_NOTIFICATIONS`; enabled authenticated `web-runtime-smoke` proves review → confirmation → backend execution for both notification mutations. Vision parity remains active.
2026-07-18 Notifications visual remediation progress: `NotificationsView` now uses the shared `AppButton` for mark-all-read and one consolidated graphite/token style contract for the attention collection, selected preview, links, borders, and responsive reflow; its route-family evidence is included in the fresh 36-route desktop+narrow capture.
2026-07-18 Thing detail visual remediation progress: `ThingDetailView` now uses the shared `AppButton` for borrow requests and one consolidated graphite detail/form contract for the detail surface, utility properties, textarea, status states, and responsive spacing; fresh route-archetype evidence passed with desktop and narrow captures, while route-level remediation remains active.
2026-07-18 Business profile visual remediation progress: `BusinessProfileView` now uses shared `AppButton` and `AppFormField` primitives for profile, gallery, business switching, archive, create, and publish/hide actions; duplicated local pill/card CSS was consolidated into one graphite form/workspace contract, and fresh route-archetype evidence passed for desktop and narrow layouts.
2026-07-18 Circles visual remediation progress: `CirclesView` now uses shared `AppButton`/`AppFormField` controls for search, requests, blocking, membership, inline edit, retry, and create flows; trust-boundary messaging, selected-circle preview, dense rows, and narrow reflow use one tokenized graphite contract, with fresh route-archetype evidence passed for desktop and narrow layouts.
2026-07-18 Business availability visual remediation progress: `BusinessAvailabilityView` now uses shared `AppButton` actions and `AppFormField`-wrapped schedule inputs, with tokenized graphite controls, dense rule rows, selected-rule preview, and narrow reflow; backend-owned availability and booking validation remain unchanged, and fresh route-archetype evidence passed for desktop and narrow layouts.
2026-07-18 Business availability exceptions visual remediation progress: `BusinessAvailabilityExceptionsView` now uses shared `AppButton` actions and `AppFormField`/`AppFormFooter` controls for exception creation and editing, while preserving backend exception precedence and booking-conflict validation; fresh route-archetype evidence passed for desktop and narrow layouts.
2026-07-18 Business offerings visual remediation progress: `BusinessOfferingsView` now uses shared `AppButton` actions and the existing shared form primitives for create/edit flows, with dense offering rows, selected preview, tokenized controls, and backend-owned pricing/booking validation unchanged; fresh route-archetype evidence passed for desktop and narrow layouts.
2026-07-18 Business owner bookings visual remediation progress: `BusinessBookingsView` now renders backend-provided `allowedActions` through shared `AppButton` tones/loading states and uses shared controls in reschedule confirmation; action authority and server-side availability checks remain unchanged, with fresh route-archetype evidence passed for desktop and narrow layouts.
2026-07-18 Chat action visual remediation progress: `ChatSurfaceView` now uses shared `AppButton` controls for chat/group entry, group creation, pagination, leave-group, and load-older actions while preserving realtime sync, attachment, message, and recovery behavior; fresh route-archetype evidence passed for desktop and narrow layouts.
2026-07-18 Things discovery visual remediation progress: `ThingsDiscoveryView` now uses shared `AppButton` controls for collection filters, listing actions, borrow requests, owner decisions, return/cancel actions, and create/edit dialogs; Vision handoff and backend borrow/archive authority remain unchanged, with fresh route-archetype evidence passed for desktop and narrow layouts.
2026-07-18 Rides visual remediation progress: `RidesView` now uses shared `AppButton` controls for route tabs, ride matching, commute preferences, preview, join/leave/start/complete/cancel lifecycle actions, and offer/edit forms; consent, circle visibility, and lifecycle validation remain backend-owned, with fresh route-archetype evidence passed for desktop and narrow layouts.
2026-07-18 Work applications visual remediation progress: `WorkQuestApplicationsView` now renders backend `allowedActions` through shared `AppButton` tones/loading states and uses shared controls for hidden-application reveal and worker replacement; lifecycle decisions and permission boundaries remain backend-authoritative, with fresh route-archetype evidence passed for desktop and narrow layouts.
2026-07-18 Personal applications visual remediation progress: `WorkApplicationsView` now renders backend `allowedActions` through shared edit/withdraw controls and uses shared form/footer and pagination primitives; application update/withdraw contracts remain unchanged, with fresh route-archetype evidence passed for desktop and narrow layouts.
2026-07-18 People profile visual remediation progress: `PeopleProfileView` now uses shared actions for backend-controlled primary, block/unblock, report, and report-form operations; trust/consent visibility and safety-report behavior remain unchanged, with fresh route-archetype evidence passed for desktop and narrow layouts.
2026-07-18 Work discovery visual remediation progress: `WorkDiscoveryView` now uses shared controls for retry, row preview, and pagination while retaining keyboard navigation, density preference, filter context, and backend search/sort semantics; fresh route-archetype evidence passed for desktop and narrow layouts.
2026-07-18 Work quest create visual remediation progress: `WorkQuestCreateView` now uses shared loading/primary submit controls inside the explicit local-draft form; Vision handoff and backend creation boundary remain unchanged, with fresh route-archetype evidence passed for desktop and narrow layouts.
2026-07-18 Work quest detail visual remediation progress: `WorkQuestDetailView` now renders backend `can()`-guarded lifecycle, term, application, edit, review, retry, and utility actions through shared controls; confirmation gates and quest lifecycle contracts remain unchanged, with fresh route-archetype evidence passed for desktop and narrow layouts.
2026-07-18 Public business visual remediation progress: `BusinessPublicView` now uses shared controls for return-to-results, offering booking, favorites, and booking request form actions; preview-before-commit booking handoff and backend validation remain unchanged, with fresh route-archetype evidence passed for desktop and narrow layouts.
2026-07-18 Customer bookings visual remediation progress: `BusinessMyBookingsView` now renders backend `allowedActions` through shared reschedule/cancel controls and uses shared form footer loading states; booking policy and server-side reschedule/cancel validation remain unchanged, with fresh route-archetype evidence passed for desktop and narrow layouts.
2026-07-18 Notification preferences visual remediation progress: `NotificationPreferencesView` now uses shared loading/primary save control while required system notice disabling remains backend-constrained; fresh route-archetype evidence passed for desktop and narrow layouts.
2026-07-18 Onboarding visual remediation progress: `OnboardingView` now uses shared controls for step continuation/completion, reset, and skip setup while preserving backend onboarding transitions and optional skip semantics; fresh route-archetype evidence passed for desktop and narrow layouts.
2026-07-18 People discovery visual remediation progress: `PeopleDiscoveryView` now uses shared secondary/primary loading controls for preview and backend-authorized connection invites while preserving trust-aware visibility and invite contracts; fresh route-archetype evidence passed for desktop and narrow layouts.
2026-07-18 Profile/location visual remediation progress: `ProfileLocationSettingsView` now uses shared danger/secondary/primary/quiet controls for avatar removal, gallery add/remove, current-location lookup, location suggestions, and profile save while preserving backend privacy and consent validation; fresh route-archetype evidence passed for desktop and narrow layouts.
2026-07-18 Chat composer visual remediation progress: `ChatSurfaceView` now uses shared controls for direct-chat candidates, error retry, message edit/reply/reaction actions, reply cancellation, attachment removal, and send loading state while preserving realtime sync, attachment, and server-authoritative message contracts; fresh route-archetype evidence passed for desktop and narrow layouts.
2026-07-18 Authenticated shell visual remediation progress: `AuthenticatedShellView` now uses shared controls for personal-context retry, mobile drawer entry/close, and focus-safe drawer restoration via the shared `AppButton` focus API; navigation, rail resizing, responsive drawer behavior, and Vision handoff remain unchanged, with fresh route-archetype evidence passed for desktop and narrow layouts.
2026-07-18 Personal collection visual remediation progress: `ActivityView` and `SavedSearchIntentsView` now use shared loading/quiet/secondary/danger controls for resume dismissal and pause/resume/delete actions while preserving backend activity and saved-intent contracts; fresh route-archetype evidence passed for desktop and narrow layouts.
2026-07-18 Business discovery visual remediation progress: `BusinessDiscoveryView` now uses the shared secondary action for row preview while preserving keyboard selection, inspect-before-open behavior, and the public-business booking handoff; fresh route-archetype evidence passed for desktop and narrow layouts.
2026-07-18 Ride detail visual remediation progress: `RideDetailView` now uses shared secondary/primary/danger loading controls for back, join, leave, start, complete, and cancel actions while preserving backend `canJoin`/`canLeave`/`canManage` gates and lifecycle transitions; fresh route-archetype evidence passed for desktop and narrow layouts.
2026-07-18 Thing detail recovery visual remediation progress: `ThingDetailView` now uses the shared quiet retry control for load failures while preserving stale/permission error messaging and borrow request authority; fresh route-archetype evidence passed for desktop and narrow layouts.
2026-07-18 Work discovery visual token remediation progress: `WorkDiscoveryView` now uses semantic control geometry/colors for Vision/offer links, filters, status surfaces, and errors instead of pill/light literals; keyboard selection, density, preview, query context, and backend search semantics remain unchanged, with fresh route-archetype evidence passed for desktop and narrow layouts.
2026-07-18 Business offerings surface remediation progress: `BusinessOfferingsView` now applies semantic graphite surface geometry to offering rows, form containers, and form controls, removing remaining card/light drift while preserving server pricing, duration, publish, and booking validation; fresh route-archetype evidence passed for desktop and narrow layouts.
2026-07-18 Business availability surface remediation progress: `BusinessAvailabilityView` now applies semantic graphite geometry to dense schedule rows and the editor surface while preserving availability precedence, capacity, and booking-conflict validation; fresh route-archetype evidence passed for desktop and narrow layouts.
2026-07-18 Business bookings surface remediation progress: `BusinessBookingsView` now applies semantic graphite geometry to booking rows, action context, and reschedule form controls while preserving backend `allowedActions` and server-side availability checks; fresh route-archetype evidence passed for desktop and narrow layouts.

This file is the persistent source of truth for open implementation and product-delivery work that should survive across Codex sessions.

Control-system improvements belong in the current work plan and are not maintained in a second control backlog.

Keep only currently open items here. When an item is implemented, remove it from the open backlog in the same change and clear matching inline `TODO(<ID>):` or `FIXME(<ID>):` references.

The target capability slice queue is generated with `make generate-target-capability-slices`.
Generated queue rows are not implementation evidence; promote a selected critical
or high row into a dedicated `docs/work/*.yaml` plan before implementation.

## Open Items

The workspace-redesign residual queue is canonical in `docs/work/workspace-redesign-todo-register.yaml` and contains exactly three active IDs: `WORKSPACE-CLOSEOUT-EVIDENCE-RECONCILIATION`, `WEB-VISION-PARITY`, and `VISION-GAP-CLOSURE`.

WORKSPACE-CLOSEOUT-EVIDENCE-RECONCILIATION [ACTIVE]: Reconcile the task-aware master evidence aggregate against a clean isolated baseline, including changed-path and evidence fingerprints.

WEB-UX-MODERNIZATION-MASTER [DONE]: Executed and verified `docs/work/frontend-ux-modernization-master.yaml` and its six child plans. Navigation IA, card/action primitives, shared form patterns, responsive Chat/Calendar, accessibility tokens/contracts, and runtime closeout are complete.

MAIN-SURFACES-MODERNIZATION [DONE]: Verified `docs/work/main-surfaces-modernization-master.yaml`; Home, Business landing, Find Work, shared clickability/action hierarchy, surface archetypes, and authenticated runtime evidence are complete.

WEB-VISION-PARITY [ACTIVE]: Make Web UI and Vision equal production clients while preserving Vision as the voice/context-aware surface. Continue the capability-by-capability audit from `docs/work/web-vision-parity-program.yaml`; the current workspace redesign is a baseline, not parity closeout.

2026-07-18 Vision parity slice: explicit prompts such as “open notifications in workspace” now receive a backend-allowlisted `workspace-v1` target (`/notifications`); ordinary read prompts remain inside Vision and unsupported targets return no link. Runtime evidence now reconciles `cross_module.shell.navigate` and `cross_module.shell.render_context`, reducing current Vision-missing coverage from 8 to 6. This is implementation progress under WEB-VISION-PARITY, not parity closeout.
2026-07-18 Vision/location slice: an explicit “open settings to use current location in workspace” prompt now hands off to `/profile/settings?location=current`; the existing Web surface owns permission, reverse lookup, recovery, and save behavior. Runtime evidence passed, reducing current Vision-missing coverage from 6 to 5. Device and native lifecycle gaps remain open.
2026-07-18 Vision/Chat slice: an explicit attachment prompt now hands off to `/chat?attach=1`; the existing Chat composer owns binary upload, expiry, and send authorization, while the handoff focuses the attachment path and explains when a conversation must be selected. Runtime evidence passed, reducing current Vision-missing coverage from 5 to 4. Vision-native binary upload remains open.
2026-07-18 Vision/profile slice: an explicit profile-visibility prompt now hands off to `/profile/settings?visibility=1` and focuses the existing exact-location scope selector. Runtime evidence passed, reducing current Vision-missing coverage from 4 to 3. Profile-wide field visibility remains open pending a dedicated backend contract.
2026-07-18 Search comparison slice: the command center can select up to three permitted mixed-family results and call backend `/search/compare`; the response preserves family/source routes and uses bounded null-safe comparable fields. Runtime evidence passed, reducing current Web and Vision missing coverage from 3 to 2. Richer family-specific comparison and broader acceptance remain open.
2026-07-18 Native handoff backend acceptance slice: fresh runtime evidence proves iPhone and Watch handoff issue/consume, redacted-context preservation, and single-use replay rejection. Native client receivers and device-level continuation remain open; `cross_module.handoff.mobile` and `cross_module.handoff.watch` are not closed.
2026-07-18 metadata reconciliation: Vision booking reschedule is implemented through the verified child plan and shared Business command/adapter, but remains open for fresh current-revision browser/runtime acceptance. The Vision backlog now records this as implemented-slice/evidence-pending rather than an absent adapter.
2026-07-18 Vision booking reschedule closeout: fixed the missing `CONFIRM_REVIEW` dispatcher branch for `RESCHEDULE_BOOKING`; fresh execution-enabled Vision browser evidence now proves review → confirmation → backend reschedule, alongside Web/API customer and owner evidence. Only the atomic `business.booking.reschedule` capability is closed; the Vision backlog remains active for its other gaps.
2026-07-18 Vision notification preference slice: normalized `in-app` prompt variants now route to `UPDATE_NOTIFICATION_PREFERENCES`; execution-enabled browser evidence proves review → confirmation → backend in-app toggle. Push/email delivery integrations remain open, so the broader capability is not closed.
2026-07-18 Vision reopen routing slice: added normalized `reopen quest` signal precedence and `CONFIRM_REVIEW` dispatch for `REOPEN_QUEST`; subsequent runtime closeout now covers the approved-applicant completed-quest path and application reset.



CAPABILITY-COVERAGE-RELIABILITY [DEFERRED]: Close the remaining partial and not-offered capabilities recorded in `docs/capability-inventory.yaml`. Resume `docs/work/capability-coverage-and-reliability.yaml` when browser/device runtime tooling or native client implementation workspace is available.

ACCOUNT-RECOVERY-CONTRACT [PARTIAL / ACTIVE]: Backend hashed single-use recovery tokens, expiry/revocation, generic request response, configured per-email/source throttling, reset mutation, and Web `/recover` + `/reset-password` routes are implemented in `docs/work/account-recovery-contract.yaml`. Delivery provider, distributed abuse coordination, valid-token browser reset proof, and mobile/Watch surfaces remain open.

PROFILE-VISIBILITY-CONTRACT [PARTIAL / ACTIVE]: Backend `PUBLIC`/`PRIVATE` description and avatar policy plus viewer DTO redaction are implemented in `docs/work/profile-visibility-contract.yaml`; Profile Settings runtime evidence now proves owner/accepted-contact/outsider behavior. Circle-scoped consent, recipient selection, Vision handoff, and mobile/Watch presentation remain open.

SEARCH-COMPARISON-CONTRACT [PARTIAL / ACTIVE]: Bounded comparable fields, privacy rules, selection limits, shared DTOs, Web selection UI, and runtime evidence are implemented. Keep the broader parity residual open for richer family-specific fields and acceptance coverage; do not re-open the completed bounded slice.

RIDE-MODULE-CONTRACT [COMPLETED]: Complete voluntary Rides module is implemented and verified through `docs/work/rides-production-completion-master.yaml`, including domain lifecycle, circle consent/privacy, backend API, Web, Vision, runtime evidence, notifications, and closeout verification.

RIDE-VISION-PARITY [COMPLETED]: Canonical Vision ride read/mutation intents, review/confirmation, read results, and recovery are implemented; tracked by `docs/work/rides-production-completion.yaml`.

RIDE-NOTIFICATION-FANOUT [COMPLETED]: Ride join/leave/update/cancel/start/complete events fan out to the shared in-app notification/news surface without embedding ride visibility details; tracked by `docs/work/rides-production-completion.yaml`.

VISION-GAP-CLOSURE [ACTIVE]: Close the remaining prioritized Vision surface gaps using the prioritized queue, the contract bundle in `docs/work/vision-deferred-contract-bundle.yaml`, and readiness gates in `docs/work/vision-gap-readiness.yaml`. All seven gates are contract-ready; promote each into an implementation plan before claiming coverage.


## Intake Rules

- Record new deferred implementation or control-system work in the appropriate persistent backlog with a stable ID before closing the change that discovered it.
- Record new persistent implementation follow-ups here with a stable uppercase ID before closing the change that discovered them.
- If a deferred follow-up also needs an inline code note, use `TODO(<ID>):` or `FIXME(<ID>):` and make sure the same ID exists in an open backlog entry.
- When a backlog item is implemented, remove it from the open backlog and clear matching inline `TODO(<ID>):` or `FIXME(<ID>):` references in the same change.
- Do not keep resolved items here as historical checkboxes. Remove them once the implementing change is complete.
