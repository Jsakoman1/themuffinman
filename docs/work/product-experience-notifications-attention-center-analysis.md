# Notifications and attention center analysis

The existing notification surface should become the canonical event feed, while Home keeps only counts/summary. Define event taxonomy, idempotency keys, grouping, read state, retention, destination authorization, and category preferences before adding fan-out from each module. Security events must remain visible even when marketing or convenience categories are muted.
