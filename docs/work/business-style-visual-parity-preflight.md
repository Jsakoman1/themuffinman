# Business-style visual parity preflight

## Starting point

The authenticated shell already supplies the fixed branded sand header, sidebar navigation, and enlarged Home logo. The Business owner workspace is the interaction reference: concise purpose, local sibling tabs, a visible next action, and quiet supporting data.

## Residual problem

Several routes still use older `SurfaceHeader`, `TaskSurface`, or bespoke detail/form layouts. They work, but their visual hierarchy, card density, action placement, and mobile stacking do not consistently communicate the same product language.

## Boundaries

This program changes presentation only. It must not alter backend contracts, permissions, route meanings, mutations, booking rules, or navigation ownership. Existing Home, Business, and prior app-wide-friendly verification remain baseline context rather than closure evidence.

## Execution readiness

The execution inventory is strictly serial. The first task validates that every later task remains atomic and bounded; no UI task begins before that task is verifier-verified. Desktop and mobile browser evidence are separate final tasks.

## Plan reconciliation

- The master declares four completed programs as baseline-only; their screenshots and tests cannot be reused as this program's completion evidence.
- The eight inventory entries map one-to-one to child-plan tasks, have one dependency chain, exact required paths, and non-recursive leaf validations.
- The scoped System Map impact report was generated at `2026-07-29T18:58:06Z` for the 28 proposed frontend and documentation paths. It confirms that only the app-shell ownership boundary and new runtime proof need review; no backend, permission, data, or capability registry change is planned.
- The atomic-task hardening, plan coverage, and recursion audits pass in preparation state. The hardening task itself remains pending because verifier-controlled work must start it before it can be marked verified.

## Start authorization

Goal pursuing may begin with the hardening task only. It must remain the sole active
inventory item until the verifier records its successful result.
