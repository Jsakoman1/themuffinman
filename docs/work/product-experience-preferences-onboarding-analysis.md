# Preferences and onboarding analysis

Preferences should be explicit durable user choices, separate from inferred Vision memory. Define safe defaults, sensitive consent versions, reset/revoke semantics, skip behavior, and backend DTO ownership. Onboarding must be resumable but never block core use; commute and location preferences require distinct consent from general personalization.
