<script setup lang="ts">
defineProps<{steps: string[]; current: number}>()
const emit = defineEmits<{select: [number]}>()
</script>
<template><nav class="setup-stepper" aria-label="Service setup steps"><button v-for="(step, index) in steps" :key="step" type="button" :class="{'setup-stepper__step--active': current === index, 'setup-stepper__step--done': current > index}" :aria-current="current === index ? 'step' : undefined" @click="emit('select', index)"><span>{{ index + 1 }}</span>{{ step }}</button></nav></template>
<style scoped>.setup-stepper{display:flex;gap:var(--space-1);overflow:auto;padding-bottom:var(--space-1);border-bottom:1px solid var(--border-subtle)}.setup-stepper__step{display:inline-flex;align-items:center;gap:var(--space-1);min-height:var(--control-height-default);padding:var(--space-1) var(--space-2);border:0;border-bottom:2px solid transparent;background:transparent;color:var(--text-muted);font:inherit;font-size:var(--text-size-meta);font-weight:var(--text-weight-semibold);white-space:nowrap;cursor:pointer}.setup-stepper__step span{display:grid;place-items:center;width:1.35rem;height:1.35rem;border:1px solid var(--border-strong);border-radius:50%;font-size:var(--text-size-label)}.setup-stepper__step--active{border-bottom-color:var(--accent);color:var(--text)}.setup-stepper__step--active span,.setup-stepper__step--done span{border-color:var(--accent);background:var(--accent);color:var(--canvas)}</style>
