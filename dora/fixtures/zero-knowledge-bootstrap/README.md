# Zero-knowledge bootstrap fixture

The test creates its source descriptor at runtime, bootstraps a blank technical
project with the GitHub Actions CI pack, and adds one declared local fixture plugin.
No MuffinMan source, PATH setup, or network download is used.
