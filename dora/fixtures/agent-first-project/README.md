# Agent-first project fixture

This fixture is a neutral application idea used to prove Dora's deterministic operating path:

`idea → bootstrap → product and domain knowledge → agent profile → built-in analysis → atomic plan → verification`

It contains no MuffinMan path, product rule, provider credential, or runtime completion claim.
