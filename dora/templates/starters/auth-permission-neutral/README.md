# Neutral authentication and permission starter

This technical starter deliberately creates no credential, default user, provider setup, role, ownership policy, endpoint, or business rule.

Before implementation, supply a separately confirmed `dora_auth_permission_starter` declaration with authentication mode, roles, ownership rule, and audit events. Review that declaration before converting it into any product behavior.
