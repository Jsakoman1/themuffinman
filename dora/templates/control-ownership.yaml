kind: dora_control_ownership
version: 1
allowed_owners: [core, adapter, extension, historical]
rules:
  - id: reusable-core
    owner: core
    subjects: [script:tools/work-check.rb]
  - id: project-extension-fallback
    owner: extension
    patterns: [script:project/**/*.rb, make:*]
