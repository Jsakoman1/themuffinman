kind: dora_repository_map
version: 1
source_roots:
  - id: backend
    path: backend
    plugins: [java_spring]
relationships:
  - from: backend
    to: documentation
    kind: documented_by
rules:
  - Source roots and relationships are project-owned declarations.
  - Output describes files and declared relationships, not product capabilities.
