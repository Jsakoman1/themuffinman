# Data safety and demo-data review templates

Complete the data-safety profile and demo-data policy with confirmed project decisions before implementing operational behavior.

Any backup, restore test, export, retention action, deletion, or database connection requires explicit approval at the time it would affect an external system. Demo data must remain separate from real data.
