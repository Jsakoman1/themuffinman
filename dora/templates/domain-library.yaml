kind: dora_domain_library
version: 1
vocabulary:
  - {id: primary-concept, description: Replace with a domain term and its meaning.}
entities:
  - {id: primary-entity, description: Replace with an entity and its ownership boundary.}
invariants:
  - {id: primary-invariant, description: Replace with a rule that must always hold.}
permission_rules:
  - {id: primary-permission, actor: Replace with an actor, action: Replace with an action, boundary: Replace with the authorization boundary.}
workflows:
  - id: primary-workflow
    initial_state: draft
    transitions:
      - {from: draft, to: active, action: activate}
acceptance_scenarios:
  - {id: primary-scenario, given: Replace with the starting condition., when: Replace with the action., then: Replace with the observable business result.}
