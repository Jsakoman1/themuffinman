kind: dora_optional_plugin_pack
version: 1
id: spring-vue-http-contract
entrypoint: dora/lib/dora/plugins/http_contract_linker.rb
inputs:
  required: [dto_glob, contract_path, frontend_glob, controller_glob, client_glob]
output:
  kind: static-http-contract-report
invariants:
  - All source roots are declared by the consumer.
  - Static links are not runtime acceptance or capability proof.
