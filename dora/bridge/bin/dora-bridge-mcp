#!/usr/bin/env ruby
# frozen_string_literal: true

require_relative "../lib/dora_bridge/server"

config_path = ARGV.first
abort "usage: dora-bridge-mcp BRIDGE_PROJECTS.yaml" unless config_path && ARGV.length == 1

DoraBridge::Server.new(DoraBridge::ProjectRegistry.load!(config_path)).run
