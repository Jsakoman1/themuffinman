kind: dora_guided_agent_entrypoint_fixture
version: 1
incomplete:
  kind: dora_guided_agent_entrypoint
  version: 1
  session:
    kind: dora_idea_interview_session
    version: 1
    project_id: household-records
    answers: []
    open_decisions: []
    completion_boundary: A guided session records confirmed answers and explicit open decisions only; it does not infer product rules or prove readiness.
  handoff:
    dora_source: reviewed-dora-source
    first_work: declare-first-capability
    starter: spring-vue-postgres-buildable
    codex_integration: true
  completion_boundary: The fixture expects one question only and no project mutation.
complete:
  kind: dora_guided_agent_entrypoint
  version: 1
  session:
    kind: dora_idea_interview_session
    version: 1
    project_id: household-records
    answers:
      - {id: target_users, value: Members of one household, source: user_confirmed}
      - {id: first_problem, value: Keep a reliable record of household items, source: user_confirmed}
      - {id: first_capability, value: Record a household item, source: user_confirmed}
      - {id: domain_concepts, value: Item record location quantity review date, source: user_confirmed}
      - {id: permission_intent, value: Household owner manages records and members can view, source: user_confirmed}
      - {id: workflow_intent, value: Draft active archived, source: user_confirmed}
      - {id: forbidden_outcomes, value: Do not change a record without confirmation, source: user_confirmed}
    open_decisions:
      - {id: offline-mode, question: "Is offline synchronization required for the first release?", source: user_confirmed, status: open}
    completion_boundary: A guided session records confirmed answers and explicit open decisions only; it does not infer product rules or prove readiness.
  handoff:
    dora_source: reviewed-dora-source
    first_work: declare-first-capability
    starter: spring-vue-postgres-buildable
    codex_integration: true
  completion_boundary: The fixture expects a review-only create-app handoff and preserves the explicit open decision.
