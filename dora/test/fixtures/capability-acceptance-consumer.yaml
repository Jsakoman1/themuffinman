kind: dora_capability_acceptance
version: 1
capability: record-supply
acceptance_statements:
  - id: record-confirmed
    expected: A user can record an explicitly permitted supply.
    confirmed: true
static_checks:
  - id: contract-review
    expected: The declared capability contract is structurally valid.
    confirmed: true
test_scenarios:
  - id: service-test
    expected: The implementation test can demonstrate the confirmed behavior.
    confirmed: true
runtime_scenarios:
  - id: local-runtime
    expected: A local runtime scenario can demonstrate the confirmed behavior.
    confirmed: true
confirmation: true
