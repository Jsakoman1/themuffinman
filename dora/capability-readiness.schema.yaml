kind: dora_capability_readiness_schema
version: 1
required_fields:
  - kind
  - version
  - capability_id
  - ready_to_implement
  - blocking_gaps
  - recommended_next_action
  - completion_boundary
completion_boundary: Capability readiness reports declared implementation gaps only; it is not a completion, runtime acceptance, or release claim.
