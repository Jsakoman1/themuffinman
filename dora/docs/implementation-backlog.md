# Dora implementation backlog

- `DORA-IMPLEMENTATION-EXCELLENCE-001` Keep the draft [implementation-excellence master](work/dora-implementation-excellence-master.yaml) and its [pre-goal atomic blueprint](work/dora-implementation-excellence-pre-goal-inventory.yaml) as the deferred planning authority for Dora truth hygiene, Codex implementation context/routing/closeout, evidence hardening, consumer contracts, and owner-readable projections. It is prepared for owner selection but cannot start until one item is freshly hardened into a verifier-valid atomic work plan; it grants no new bridge, runner, consumer-project, release, or GitHub authority.
- `DORA-CONTROL-STATE-003` Assess whether verifier closeout can require or safely perform the matching terminal execution-inventory state transition. The control-state hygiene slice proved the current verifier records verified work while a serial inventory can remain `active`; any future change must preserve historical evidence, avoid automatic reconciliation of unrelated records, and retain Doctor's explicit conflict signal for mismatches.
- `DORA-BRIDGE-WRITE-001` Complete V2 only after a fresh ChatGPT developer-mode observation discovers `create_handoff` and proves a real create → owner-run claim → Dora verification → sanitized readback flow. Bridge V1 remains read-only.
- `DORA-BRIDGE-REMOTE-001` Evaluate ChatGPT remotely starting, steering, or controlling Codex only as a new security program with separate authorization, process-isolation, and auditability decisions.
