kind: work
version: 1
id: dora-greenfield-discovery-delivery-coordinator-preflight
title: Require synthesis failure evidence before adding a discovery coordinator
status: intentionally_deferred
baseline: 4b07fbd00c5d59d2788346cddbe4c44606f8fdff
strict_verification: true
serial_task_execution: true
execution_inventory: docs/work/dora-greenfield-discovery-delivery-coordinator-preflight-execution-inventory.yaml
planning_item:
  phase: later_optimization
  gate: later-optimization-need
  reason: A coordinator is a scheduling/state risk, not a default discovery feature.
tasks:
- id: record-discovery-coordinator-need-preflight
  title: Compare simple synthesis with an unimplemented coordinator proposal
  type: planning
  status: intentionally_deferred
  inventory_item: discovery-coordinator-preflight
  observable_outcome: A planning-only comparison rejects a coordinator unless repeated owner-reviewed
    examples show that deterministic synthesis loses material conflicts across independent discovery lanes.
  dependencies:
  - role-context-pack-preflight
  evidence_boundary:
  - Recorded owner-reviewed synthetic examples and existing synthesis test fixtures only.
  - No queue, worker, scheduler, subprocess, external call, consumer mutation, GitHub action,
    runner invocation, or remote-agent start.
  paths:
  - docs/work/dora-greenfield-discovery-delivery-12-coordinator-preflight.yaml
  - test/discovery_synthesis_test.rb
  required_paths:
  - docs/work/dora-greenfield-discovery-delivery-12-coordinator-preflight.yaml
  - test/discovery_synthesis_test.rb
  validation: ruby test/discovery_synthesis_test.rb
  negative_authority_proof: Preflight proves no coordinator state/execution is introduced and cannot
    mutate decision/status/code/consumer/GitHub or start local/remote agents.
evidence: []
