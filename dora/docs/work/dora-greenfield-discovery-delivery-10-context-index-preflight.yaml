kind: work
version: 1
id: dora-greenfield-discovery-delivery-context-index-preflight
title: Require evidence before adding a derived greenfield context index
status: intentionally_deferred
baseline: 4b07fbd00c5d59d2788346cddbe4c44606f8fdff
strict_verification: true
serial_task_execution: true
execution_inventory: docs/work/dora-greenfield-discovery-delivery-context-index-preflight-execution-inventory.yaml
planning_item:
  phase: later_optimization
  gate: later-optimization-need
  conditional_evidence: Two reviewed greenfield sessions demonstrate a cited context-selection gap.
  reason: Prevent Project Context Graph from becoming a new graph database or canonical model.
tasks:
- id: record-derived-context-index-need-preflight
  title: Decide whether existing read models fail a concrete context-selection need
  type: planning
  status: intentionally_deferred
  inventory_item: derived-context-index-preflight
  observable_outcome: A read-only preflight compares ProjectReadModel, AssistantContextView,
    ChangeImpact, and simple synthesis against named observed gaps and either rejects or narrowly scopes an index.
  dependencies:
  - delivery-scenario-handoff
  evidence_boundary:
  - Existing local read-model contracts and synthetic/reviewed examples only.
  - No index, graph database, cache, consumer read, source mutation, GitHub action,
    external request, runner invocation, or remote-agent start.
  paths:
  - docs/work/dora-greenfield-discovery-delivery-10-context-index-preflight.yaml
  - test/project_read_model_test.rb
  required_paths:
  - docs/work/dora-greenfield-discovery-delivery-10-context-index-preflight.yaml
  - test/project_read_model_test.rb
  validation: ruby test/project_read_model_test.rb
  negative_authority_proof: Evidence proves the preflight adds no state and cannot alter status,
    decisions, code, consumers, GitHub, runner, or remote-agent state.
evidence: []
