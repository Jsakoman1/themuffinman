kind: dora_release_manifest
version: 1
release:
  version: v1.11.3
  source_commit: c7a286e6bcc5ffe0f0693af42db23baf2f0ce2ff
  release_type: patch
  published_at: '2026-08-12T13:00:00Z'
scope:
  changed_capabilities:
  - Dora source checksums exclude `.git`, `.idea` and `.DS_Store`, matching vendored package containment.
  - A reviewed source pin is stable across local source-control and IDE metadata changes while still detecting released-source tampering.
  idc_impact: IDC runtime behavior is unchanged; the release only hardens source provenance for package distribution.
  compatibility_posture: All real consumer source descriptors should target this tag rather than v1.11.1 or v1.11.2.
consumer_requirements:
  package_update: explicit-owner-selection-only
  adapter_change_required: true
  required_validations: [upgrade_preview, dora_doctor, git_diff_check, consumer_declared_checks]
  rollback: Restore the local upgrade backup before commit or revert the isolated consumer release commit after commit.
boundaries:
  - The release manifest does not claim that any consumer is updated, compatible, deployed or verified.
  - IDC remains opt-in; a Dora package update does not start IDC or create a Dora write path.
  - The release process does not discover consumer repositories or modify one without explicit owner approval.
