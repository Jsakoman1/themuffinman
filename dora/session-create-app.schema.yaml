kind: dora_session_create_app_schema
version: 1
required_fields: [kind, version, session, dora_source, first_work]
invariants:
  - Every required interview answer is user_confirmed before conversion.
  - Open decisions remain unanswered decisions and are never converted into product rules.
  - Conversion creates declared starting context only; it does not create implementation.
