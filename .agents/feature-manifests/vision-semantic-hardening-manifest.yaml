featureId: vision-semantic-hardening
title: Vision semantic hardening
workflowTier: tier3-high-risk-multi-layer
manifestDecision: required
status: draft
riskTier: medium
changeMode: major-change
changeImpact: logic-drift
changeProfiles:
  - backend-logic
planFile: .agents/vision-semantic-hardening-plan.md
summary:
  - Corrected family-specific alias normalization in vision entity resolution.
  - Added regression tests for quest, circle, and application alias lookups.
  - Canonicalized semantic envelope slot candidates and target entity queries before resolver lookup and replay capture.
validationEvidence:
  commands:
    - command: ./mvnw test -Dtest=SemanticAliasRegistryTest,VisionCapabilityPreviewServiceAliasResolutionTest,VisionEntityResolverRegistryTest
      scope: backend
      result: passed
      summary: Focused alias-resolution regression tests passed.
    - command: ./mvnw test -Dtest=VisionPromptUnderstandingServiceTest
      scope: backend
      result: passed
      summary: Prompt understanding tests passed after the semantic resolver updates.
generatedArtifacts:
  refreshedPaths:
    - docs/generated/local-tooling/plan-index-summary.md
    - docs/generated/local-tooling/control-start-summary.md
  notApplicableReason: No generated artifact refresh was needed beyond the plan/control summaries already regenerated by the workflow.
planCompletion:
  reviewed: true
  openTasks: 0
  summary: The alias-resolution hardening slice is complete and validated.
closeoutDecision:
  status: ready
  reason: The semantic hardening slice is implemented and test-validated, so the manifest can be used as the current completed reference point.
docDelta:
  behaviorChanged:
    - Vision entity resolution now normalizes quest, circle, application, and user queries with the correct family-specific alias set.
    - Vision semantic envelopes now carry canonicalized slot candidates and target entity queries for replay, resolver, and DTO consistency.
  docsUpdated:
    - docs/domain-technical.md
    - docs/generated/local-tooling/plan-index-summary.md
    - docs/generated/local-tooling/control-start-summary.md
  intentionallyUnchanged:
    - Frontend behavior.
    - OpenAI prompt contract structure.
backlog:
  reviewed: true
  createdIds: []
  resolvedIds: []
checklist:
  tempPlanCreated: true
  codeImplemented: true
  backendTestsPassed: true
  frontendValidationPassed: false
  docsSynced: false
  agentModelSynced: false
  destructivePolicyChecked: true
  multilingualCoverageChecked: true
artifacts:
  codePaths:
    - apps/themuffinman/src/main/java/com/themuffinman/app/vision/service/VisionCapabilityPreviewService.java
    - apps/themuffinman/src/main/java/com/themuffinman/app/vision/service/VisionPromptUnderstandingService.java
  docPaths:
    - docs/domain-technical.md
    - docs/generated/local-tooling/plan-index-summary.md
    - docs/generated/local-tooling/control-start-summary.md
  testPaths:
    - apps/themuffinman/src/test/java/com/themuffinman/app/vision/service/VisionCapabilityPreviewServiceAliasResolutionTest.java
    - apps/themuffinman/src/test/java/com/themuffinman/app/semantic/SemanticAliasRegistryTest.java
  generatorCommands:
    - make plan-index
    - make control-start
  auditCommands:
    - ./mvnw test
    - ./mvnw test -Dtest=SemanticAliasRegistryTest,VisionCapabilityPreviewServiceAliasResolutionTest,VisionEntityResolverRegistryTest
    - ./mvnw test -Dtest=VisionPromptUnderstandingServiceTest
