#!/usr/bin/env sh
set -eu

project_root=$(CDPATH= cd "$(dirname "$0")/.." && pwd)
package_root="$project_root/dora"

if [ ! -x "$package_root/bin/dora" ]; then
  echo "Dora launcher failed: pinned Dora package is unavailable at dora/bin/dora" >&2
  exit 1
fi

exec "$package_root/bin/dora" "$@"
